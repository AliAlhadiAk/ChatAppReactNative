import { View } from "react-native"
import { Divider } from "react-native-elements"
import tw from "tailwind-react-native-classnames"
const Devider:React.FC = () => {
    return (
      <View style={tw`p-[1px] w-full bg-gray-200`}/>
    )
  }
  export default Divider